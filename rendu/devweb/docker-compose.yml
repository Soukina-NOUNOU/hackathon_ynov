version: "3.9"

services:
  devweb:
    build: .
    container_name: techcorp-chat
    ports:
      - "5000:5000"
    environment:
      - OLLAMA_URL=${OLLAMA_URL:-http://host.docker.internal:11434}
      - MODEL_NAME=${MODEL_NAME:-tinyllama:1.1b}
    restart: unless-stopped
